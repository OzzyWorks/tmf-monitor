# 📊 TMF爆発察知ツール - プロジェクト構成図

## 🏗️ アーキテクチャ概要

```
┌─────────────────────────────────────────────────────────────┐
│                    GitHub Actions (Cron)                     │
│                     毎日JST 7:00実行                          │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   Python実行環境                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ data_fetch.py│  │  scoring.py  │  │  notify.py   │      │
│  │ データ取得    │→│ スコアリング  │→│ Slack通知    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                              │                               │
│                              ▼                               │
│                    ┌──────────────┐                         │
│                    │  render.py   │                         │
│                    │ HTML/JSON生成│                         │
│                    └──────────────┘                         │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│              GitHub Pages (docs/)                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ index.html   │  │  data.json   │  │previous.json │      │
│  │ ダッシュボード│  │  最新データ   │  │  前回データ  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                      │
                      ▼
              ユーザー（ブラウザ）
```

## 📁 ディレクトリ構造

```
tmf-monitor/
│
├── .github/
│   └── workflows/
│       └── tmf.yml                  # GitHub Actions設定
│                                    # - cron定義
│                                    # - Python実行環境
│                                    # - 自動commit/push
│
├── src/                             # Pythonソースコード
│   ├── main.py                      # エントリーポイント
│   │                                # - 全モジュール統合
│   │                                # - 実行フロー制御
│   │
│   ├── data_fetch.py                # データ取得
│   │                                # - FRED API (金利)
│   │                                # - Yahoo Finance (S&P500)
│   │                                # - VIX取得
│   │
│   ├── scoring.py                   # スコアリング
│   │                                # - TMFスコア計算 (0-100)
│   │                                # - ステータス判定
│   │                                # - ブースト条件チェック
│   │
│   ├── notify.py                    # Slack通知
│   │                                # - ステータス変化通知
│   │                                # - 定期サマリー通知
│   │                                # - Incoming Webhook使用
│   │
│   └── render.py                    # HTML/JSON生成
│                                    # - ダッシュボード生成
│                                    # - データJSON出力
│
├── docs/                            # GitHub Pages公開ディレクトリ
│   ├── index.html                   # ダッシュボード（自動生成）
│   │                                # - リアルタイム表示
│   │                                # - レスポンシブデザイン
│   │                                # - Vanilla JS
│   │
│   ├── data.json                    # 最新データ（自動生成）
│   │                                # - TMFスコア
│   │                                # - カテゴリ別スコア
│   │                                # - 生データ
│   │
│   └── previous.json                # 前回データ（自動生成）
│                                    # - ステータス変化検知用
│
├── requirements.txt                 # Python依存関係
│                                    # - requests (データ取得)
│
├── README.md                        # プロジェクト説明
├── SETUP.md                         # セットアップガイド
└── .gitignore                       # Git除外設定
```

## 🔄 データフロー

### 1. データ取得フェーズ
```
FRED API ──────┐
(金利データ)     │
               ├──→ data_fetch.py ──→ 生データ
Yahoo Finance ─┤
(S&P500/VIX)   │
───────────────┘
```

### 2. スコアリングフェーズ
```
生データ
  │
  ├─→ 金利系スコア (40%)
  │   ├─ 10年債利回り (35%)
  │   ├─ 30年債利回り (35%)
  │   └─ 金利変化率 (30%)
  │
  ├─→ リスクオフスコア (60%)
  │   ├─ VIX (50%)
  │   └─ S&P500乖離率 (50%)
  │
  └─→ TMFスコア (0-100)
      └─→ ステータス判定
          ├─ 🟢 通常 (0-39)
          ├─ ⚠️ 前兆 (40-64)
          ├─ 🚨 警戒 (65-79)
          └─ 💥 直前 (80-100)
```

### 3. 通知・出力フェーズ
```
スコア結果
  │
  ├─→ Slack通知
  │   ├─ ステータス変化通知（変化時のみ）
  │   └─ 定期サマリー（毎日）
  │
  └─→ ファイル生成
      ├─ docs/data.json
      ├─ docs/index.html
      └─ docs/previous.json
```

## 🎯 監視指標詳細

### 金利系指標（40%）

| 指標 | データソース | 重み | 評価基準 |
|-----|-------------|-----|---------|
| 10年債利回り | FRED (DGS10) | 35% | 低いほど高スコア |
| 30年債利回り | FRED (DGS30) | 35% | 低いほど高スコア |
| 金利変化率 | FRED計算 | 30% | 急低下で高スコア |

### リスクオフ指標（60%）

| 指標 | データソース | 重み | 評価基準 |
|-----|-------------|-----|---------|
| VIX | FRED (VIXCLS) | 50% | 高いほど高スコア |
| S&P500乖離率 | Yahoo Finance | 50% | マイナス乖離で高スコア |

### 補助条件（ブースト）

| 条件 | 倍率 | 説明 |
|-----|------|------|
| 金利2週連続急低下 | 1.15x | -0.5%以上の低下 |
| VIX高騰 + S&P500急落 | 1.20x | VIX>20 & 200DMA-2%以下 |

## 🔐 環境変数・Secrets

### GitHub Actions環境変数
```yaml
GITHUB_REPOSITORY: リポジトリ名（自動設定）
GITHUB_TOKEN: プッシュ権限（自動設定）
```

### ユーザー設定Secrets
```yaml
SLACK_WEBHOOK_URL: Slack Incoming Webhook URL（オプション）
```

## 📊 出力ファイル仕様

### data.json構造
```json
{
  "last_updated": "ISO8601形式",
  "date": "YYYY-MM-DD",
  "score": 0-100,
  "status": {
    "level": "normal/precursor/alert/imminent",
    "label": "日本語ラベル",
    "emoji": "絵文字",
    "color": "カラーコード"
  },
  "category_scores": {
    "interest_rate": {...},
    "risk_off": {...}
  },
  "boost_conditions": {...},
  "signals": ["シグナル配列"],
  "raw_data": {...}
}
```

## 🕐 実行スケジュール

```
GitHub Actions Cron:
  - デフォルト: 毎日 JST 7:00 (UTC 22:00)
  - 変更可能: .github/workflows/tmf.yml

実行時間: 約30秒〜1分
  - データ取得: 5-10秒
  - スコアリング: <1秒
  - 通知送信: 1-2秒
  - ファイル生成: <1秒
```

## 🌐 公開URL

```
GitHub Pages:
  https://[username].github.io/[repo-name]/

更新頻度: 毎日自動更新
  - GitHub Actions実行後、数分で反映
  - ブラウザ自動更新: 5分ごと（JS実装）
```

## 💰 コスト

```
完全無料:
  ✅ GitHub Actions: 月2,000分無料（本ツールは約30分/月使用）
  ✅ GitHub Pages: 無料
  ✅ データAPI: 無料（FRED, Yahoo Finance）
  ✅ Slack Incoming Webhook: 無料
```

## 🔧 カスタマイズポイント

### 簡単（初心者向け）
- [ ] cron実行時間変更
- [ ] Slack通知ON/OFF
- [ ] スコア閾値調整

### 中級（コード編集）
- [ ] 重み付け変更
- [ ] 新規指標追加
- [ ] ダッシュボードデザイン変更

### 上級（アーキテクチャ変更）
- [ ] データベース追加
- [ ] リアルタイム監視
- [ ] 複数ETF対応

---

**📝 作成日: 2024-01-06**
**🔄 最終更新: 自動生成**
